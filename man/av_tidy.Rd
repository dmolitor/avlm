% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/av_lm.R
\name{av_tidy}
\alias{av_tidy}
\title{Convert arbitrary model objects to anytime-valid counterparts}
\usage{
av_tidy(model, g = 1, alpha = 0.05, conf.int = TRUE, ...)
}
\arguments{
\item{model}{A model object from a linear model fit.}

\item{g}{An integer precision parameter for anytime-valid inference. Default is 1.}

\item{alpha}{The size of the test. Defaults to 0.05.}

\item{conf.int}{Logical indicating whether or not to include a confidence interval
in the tidied output. Defaults to TRUE.}

\item{...}{Additional arguments passed to \code{generics::tidy}.}
}
\value{
A \code{tibble} with tidied model estimates adjusted to be anytime-valid.
}
\description{
Converts a model object into an anytime-valid version by tidying the model
and utilizing the corresponding estimated parameters. The resulting adjusted
estimates support anytime-valid inference.
}
\examples{
if (requireNamespace("broom", quietly = TRUE)) {
  # Fit a linear model on the built-in mtcars dataset
  fit <- lm(mpg ~ wt + hp, data = mtcars)

  # Convert the standard lm coefficient estimates to an anytime-valid tidy tibble
  av_fit <- av_tidy(fit, g = 1)

  # Print the anytime-valid estimates
  print(av_fit)
}

}
